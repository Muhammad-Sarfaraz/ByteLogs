# Typescript

#### Installation:
Globally install the typescript:

```  npm i -g typescript ```

Ts-Node:

``` npm install -g ts-node typescript '@types/node' ```

Run Typescript:

``` ts-node typescript.ts ```
