# AWS Systems Manager (SSM)

#### SSM parameter store
AWS Systems Manager (SSM) Parameter Store provides secure, hierarchical storage for configuration data and secrets (via KMS encryption), allowing applications to retrieve data via API or CLI.
